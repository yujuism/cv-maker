{
  "rules": {
    "users": {
      "$userId": {
        "cvs": {
          "$cvId": {
            ".read": true,
            ".write": "auth != null && auth.uid === $userId"
          }
        }
      }
    }
  }
}
